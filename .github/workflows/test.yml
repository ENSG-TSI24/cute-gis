name: Back Test

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    # Étape 1 : Récupérer le code
    - name: Checkout repository
      uses: actions/checkout@v4

    # Étape 2 : Vérifier la structure des fichiers
    - name: Check file structure
      run: |
        echo "Current directory:"
        pwd
        echo "Files in directory:"
        ls -R

    # Étape 3 : Installer les dépendances
    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt install -y build-essential cmake qtbase5-dev qt5-qmake
        sudo apt-get install libgdal-dev

    # Étape 4 : Configurer et compiler le projet
    - name: Configure and Build
      run: |
        mkdir -p cute-gis/build
        cmake -S cute-gis -B cute-gis/build -DCMAKE_BUILD_TYPE=Release
        cmake --build cute-gis/build -- -j$(nproc)
